pub fn init() {
    // Background polling and task scheduling — implemented in Stage 4
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn init_does_not_panic() {
        init();
    }
}
