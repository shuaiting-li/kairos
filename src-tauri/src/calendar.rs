pub fn init() {
    // Calendar read/write via Google Calendar and Microsoft Graph — implemented in Stage 5
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn init_does_not_panic() {
        init();
    }
}
